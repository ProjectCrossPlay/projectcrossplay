{
  "name": "crossplay-monorepo",
  "private": true,
  "packageManager": "pnpm@11.1.1",
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build && rm -rf examples/demo-web/node_modules && pnpm install --frozen-lockfile",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "docs:api": "typedoc"
  },
  "devDependencies": {
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^5.5.4"
  }
}
