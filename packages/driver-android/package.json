{
  "name": "@projectcrossplay/driver-android",
  "version": "0.1.0",
  "description": "CrossPlay driver-android (implementation lands per sprint plan)",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "engines": {
    "node": "^20.19.0 || ^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint configured in Sprint 2'"
  },
  "dependencies": {
    "@projectcrossplay/core": "workspace:*",
    "appium-uiautomator2-server": "10.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.5.4",
    "vitest": "^3.2.7"
  }
}
