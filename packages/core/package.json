{
  "name": "@crossplay/core",
  "version": "0.1.0-dev.0",
  "description": "CrossPlay core: unified test API, selector engine, auto-wait engine, trace writer, driver contract",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'no tests yet (Sprint 2)'",
    "lint": "echo 'lint configured in Sprint 2 (B-020)'"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
