packages:
  - packages/*
  - spikes/*
  - examples/*
  # RN app manages its own npm node_modules (RN's gradle plugin expects that layout)
  - '!examples/demo-android-rn'
allowBuilds:
  esbuild: true
overrides:
  vite: '>=6.4.3'
